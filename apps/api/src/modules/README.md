# Backend Domain Modules

Governed Business-domain NestJS modules live under this directory using the approved `domain`, `application`, `infrastructure`, and `presentation` boundaries.

No Business module is created by application initialization. Add a module only when current Business scope and Requirements authorize the implementation.
