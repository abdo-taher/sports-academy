# Frontend Features

Substantial governed Frontend features live under `features/<feature>/` using the approved feature structure.

No Product feature is created by application initialization. Add feature code only from approved Requirements, UX flows, permissions, and API contracts.
