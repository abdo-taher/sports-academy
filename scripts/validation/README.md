# Validation launch adapters

Validation logic belongs exclusively to `packages/project-validator/`. This directory is reserved for small documented launch/support adapters if a future environment cannot call the package CLI directly. It must never contain a second validator implementation.
