# Current Release UX Flow Catalog

UX exposes only business actions allowed by current state, relationship, scope and permission. It never invents a policy value.

| UX ID | Capability | Screen / Entry Point | Allowed and Disabled Actions | Validation / Error / Empty / Loading | Approval / Confirmation / Success / History |
| --- | --- | --- | --- | --- | --- |
| UX-CAP-01 | CAP-01 | Governance source/decision screen; non-authoritative warning | Show allowed actions from PERM-CAP-01; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-02 | CAP-02 | Academy/Branch status screens with allowed actions, impact review and history | Show allowed actions from PERM-CAP-02; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-03 | CAP-03 | Configuration catalog/editor with scope, precedence, effective date and audit preview | Show allowed actions from PERM-CAP-03; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-04 | CAP-04 | Approval inbox, delegation editor, before/after and decision history | Show allowed actions from PERM-CAP-04; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-05 | CAP-05 | Catalog and Branch availability screens; unavailable options hidden/disabled with reason | Show allowed actions from PERM-CAP-05; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-06 | CAP-06 | Lead funnel, Trial booking/status, duplicate resolution and enrollment screens | Show allowed actions from PERM-CAP-06; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-07 | CAP-07 | Guardian relationship/access/billing screen with minor validation and history | Show allowed actions from PERM-CAP-07; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-08 | CAP-08 | Coach scope/qualification/conflict screen and future reassignment flow | Show allowed actions from PERM-CAP-08; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-09 | CAP-09 | Group roster, calendar, conflict, Session cancellation/reschedule screens | Show allowed actions from PERM-CAP-09; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-10 | CAP-10 | Subscription creation/details/eligibility screen with disabled reasons | Show allowed actions from PERM-CAP-10; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-11 | CAP-11 | Payment timeline, clearance, escalation, Discount approval and compensation screens | Show allowed actions from PERM-CAP-11; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-12 | CAP-12 | Attendance roster/check-in, funding reason, correction and Excuse approval screens | Show allowed actions from PERM-CAP-12; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-13 | CAP-13 | Evaluation editor/review/publication/movement screens with comparison/history | Show allowed actions from PERM-CAP-13; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-14 | CAP-14 | Freeze request/review/timeline and Resume controls with projected expiry | Show allowed actions from PERM-CAP-14; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-15 | CAP-15 | Renewal quote/clearance/carry-over/overlap resolution screen | Show allowed actions from PERM-CAP-15; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-16 | CAP-16 | Typed Transfer wizard, eligibility/impact review, approval and history | Show allowed actions from PERM-CAP-16; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-17 | CAP-17 | Active-relationship review, Archive confirmation, historical view and Return checklist | Show allowed actions from PERM-CAP-17; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-18 | CAP-18 | Composer, audience preview, delivery state, official report and comments views | Show allowed actions from PERM-CAP-18; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-19 | CAP-19 | Document/form upload, validation, status, version and evidence history | Show allowed actions from PERM-CAP-19; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
| UX-CAP-20 | CAP-20 | Roadmap label only; no current operational screen | Show allowed actions from PERM-CAP-20; disable invalid state/scope/relationship with reason | Inline rule/config validation; deterministic errors; empty state explains missing prerequisite; loading prevents duplicate action | Impact preview and confirmation; approval status when sensitive; success reference; timeline/history link |
